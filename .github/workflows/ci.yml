name: Test build Docker image

on:
  push:
    # branches:
    #   - 'release-**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build image
        run: docker build ./docker/ci --file Dockerfile --tag speedtest-tracker
